<Window x:Class="PRBD_2S_Aurélie.MainView"
        x:Name="win"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PRBD_2S_Aurélie"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <StackPanel>
        <TextBlock Text="List of all members" FontSize="16" FontWeight="Bold"/>
        <StackPanel Orientation="Horizontal">
            <TextBox Text="{Binding Filter}" Width="150"/>
            <Button Content="Search" Command="{Binding ApplyFilter}"/>
        </StackPanel>
        <ListView ItemsSource="{Binding Members}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Pseudo}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </StackPanel>
</Window>
